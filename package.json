{
  "name": "ui-toolkit-lib-poc",
  "version": "0.0.1-alpha",
  "description": "Proof of concept for Vue-based UI component frameworks",
  "scripts": {
    "build": "rm -rf ./lib/* && ./node_modules/.bin/babel ./src/ --source-maps-inline --copy-files -d ./lib",
    "prepare": "npm run build",
    "publish": "git push --follow-tags origin master",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/makiten/ui-toolkit-lib-poc.git"
  },
  "files": [
    "lib/"
  ],
  "author": "",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/makiten/ui-toolkit-lib-poc/issues"
  },
  "homepage": "https://github.com/makiten/ui-toolkit-lib-poc#readme",
  "peerDependencies": {
    "vue": ">=2.5.0",
    "vuetify": ">=1.3.0"
  },
  "devDependencies": {
    "@babel/cli": "^7.1.5",
    "@babel/core": "^7.1.6",
    "@babel/preset-env": "^7.1.6",
    "babel-minify": "^0.5.0",
    "babel-preset-minify": "^0.5.0",
    "eslint": "^5.9.0",
    "standard": "^12.0.1"
  },
  "dependencies": {
    "ejs": "^2.6.1",
    "fs-extra": "^7.0.1"
  }
}
